{
  "name": "lk.yl3.lv",
  "version": "1.0.0",
  "description": "Some ham notes",
  "main": "index.js",
  "license": "MIT",
  "private": false,
  "scripts": {
    "dev": "vuepress dev src",
    "push": " rm -r docs && vuepress build src --dest docs && cp src/CNAME docs/CNAME  &&git add . && git commit -m 'Push' && git push origin main "
  },
  "dependencies": {
    "vuepress": "^1.7.1",
    "vuepress-theme-meteorlxy": "^1.9.0"
  }
}
